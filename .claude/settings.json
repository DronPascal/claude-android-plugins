{
  "_comment": "Project-level settings for claude-android-plugins. PreToolUse hook enforces plugin version bumps on commit (see CLAUDE.md).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR}/.claude/hooks/check-version-bump.sh\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
